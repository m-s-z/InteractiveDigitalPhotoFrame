@model WebApplication.ViewModels.CloudViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Cloud Accounts</h2>
<ul class="list-group">
    @foreach (var cloud in Model.Clouds)
    {
        <li class="list-group-item">
            <span>@cloud.Provider.ToString()</span>
            <span>@cloud.Login</span>
            <p class="btn btn-default">@Html.ActionLink("Delete","Delete", "Cloud", new { cloudId = cloud.CloudId }, null)</p>
        </li>
    }
</ul>
<p class="btn btn-default">@Html.ActionLink("New Cloud", "NewCloud", "Cloud", new { area = "" }, null)</p>


